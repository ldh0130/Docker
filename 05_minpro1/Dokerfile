FROM centos:7

RUN yum -y update && yum -y install vsftpd && yum clean all
COPY vsftpd.conf.centos7.template /etc/vsftpd/vsftpd.conf

VOLUME /var/ftp/pub
RUN cp /etc/passwd /var/ftp/pub/WelcomeToMyServer.txt

EXPOSE 21 20

ENTRYPOINT /usr/sbin/vsftpd
